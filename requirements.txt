# GUI
PyQt5>=5.15.11

# Browser automation
selenium>=4.46.0        # Selenium Manager auto-downloads the driver (>=4.6)

# HTTP / parsing
requests>=2.34.2
beautifulsoup4>=4.15.0

# Web UI (HTML front-end)
flask>=3.1.3

# Multi-LLM vulnerability-analysis agent (/test) - optional. Free-tier providers:
#   Gemini (google-genai) + OpenAI-compatible (Groq/Cerebras/Mistral/OpenRouter/OpenAI)
google-genai>=2.15.0
openai>=2.50.0

# SAST source-code classifier (/sast, Track 2) - OPTIONAL, heavy.
# The heuristic CWE rule engine works with no extra deps. The CodeBERT /
# GraphCodeBERT classifier (core/sast + tools/train_sast.py) needs:
#   pip install torch transformers
# Not bundled into the .exe (multi-GB); install on a dev machine to train/run it.
# torch>=2.2.0
# transformers>=4.40.0
