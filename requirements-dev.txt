# Development & CI tooling (install with: pip install -r requirements-dev.txt)
-r requirements.txt

# Testing
pytest>=9.1.1
pytest-cov>=7.1.0

# Linting / formatting
ruff>=0.16.0

# Security analysis
bandit[toml]>=1.9.4
pip-audit>=2.10.1
