# Development & CI tooling (install with: pip install -r requirements-dev.txt)
-r requirements.txt

# Testing
pytest>=7.4
pytest-cov>=4.1

# Linting / formatting
ruff>=0.6

# Security analysis
bandit[toml]>=1.7
pip-audit>=2.7
